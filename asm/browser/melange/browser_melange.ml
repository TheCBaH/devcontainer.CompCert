(* The Melange adapter (.ai/asm_plan.md §11.6).

   Filled in at M0.5 alongside browser/jsoo. Note the two trees: this is the
   adapter *package*, mirrored for the Melange build under melange/browser/
   like every other production package. *)

type nothing = |
