; Melange build of browser/jsoo. Same sources, copied rather than duplicated;
; see melange/dune for why the tree is mirrored at all.

(copy_files#
 (files ../../../browser/jsoo/*.ml))

(library
 (name browser_jsoo)
 (modes byte native melange)
 (libraries driver)
 (enabled_if
  (= %{env:ASM_MELANGE=false} true)))
