; Target-level tests. No fixtures, no toolchain, no emulator: everything here
; is decided by the target descriptions themselves, so it runs in every leg.

(library
 (name test_targets)
 (modules test_targets test_components)
 (modes byte native)
 (libraries
  driver
  image
  asm_core
  target_intf
  codec
  foundation
  arm
  aarch64
  x86_32
  x86_64
  riscv32_encode
  riscv64_encode
  riscv_family_encode
  x86_32_encode
  x86_64_encode
  target_component
  fmt)
 (inline_tests)
 (preprocess
  (pps ppx_expect))
 (enabled_if
  (<> %{env:ASM_MELANGE=false} true)))
